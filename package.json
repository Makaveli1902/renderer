{
  "scripts": {
    "dev": "vite", 
    "build": "vite build",
    "preview": "vite preview", 
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "three": "^0.173.0"
  },
  "devDependencies": {
    "@types/three": "^0.173.0",
    "vite": "^6.2.0"
  }
}
